import React, {Component} from "react";
//React component to manage data in CRUD application

export default class StudentList extends Component{
    render(){
        return(<div><p>React Student List Component!</p></div>);
    }
}